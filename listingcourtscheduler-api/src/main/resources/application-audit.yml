# cp-audit-filter-springboot
# OpenAPI spec is required even when audit.http.enabled=false (library bean creation).
audit:
  http:
    enabled: ${COURTSCHEDULER_HTTP_AUDIT_ENABLED:false}
    openapi-rest-spec: ${COURTSCHEDULER_REST_SPECIFICATION:openapi/courtscheduler-api.openapi.yml}

# CP Audit (Artemis JMS) — required by cp-audit-filter-springboot even when audit.http.enabled=false
cp:
  audit:
    enabled: ${COURTSCHEDULER_AUDIT_ENABLED:false}
    high-availability: ${COURTSCHEDULER_ARTEMIS_HA:false}
    hosts:
      - ${COURTSCHEDULER_ARTEMIS_HOST_PRIMARY:localhost}
      - ${COURTSCHEDULER_ARTEMIS_HOST_SECONDARY:}
    port: ${COURTSCHEDULER_ARTEMIS_PORT:61616}
    user: ${COURTSCHEDULER_ARTEMIS_USER:}
    password: ${COURTSCHEDULER_ARTEMIS_PASSWORD:}
    ssl-enabled: ${COURTSCHEDULER_ARTEMIS_SSL_ENABLED:false}
    client-auth-required: ${COURTSCHEDULER_ARTEMIS_CLIENT_AUTH_REQUIRED:false}
    keystore: ${COURTSCHEDULER_ARTEMIS_KEYSTORE:}
    keystore-password: ${COURTSCHEDULER_ARTEMIS_KEYSTORE_PASSWORD:}
    truststore: ${COURTSCHEDULER_ARTEMIS_KEYSTORE:}
    truststore-password: ${COURTSCHEDULER_ARTEMIS_KEYSTORE_PASSWORD:}
    verify-host: ${COURTSCHEDULER_ARTEMIS_VERIFY_HOST:false}
    jms:
      reconnect-attempts: ${COURTSCHEDULER_JMS_RECONNECT_ATTEMPTS:-1}
      initial-connect-attempts: ${COURTSCHEDULER_JMS_INITIAL_CONNECT_ATTEMPTS:10}
      retry-interval-ms: ${COURTSCHEDULER_JMS_RETRY_INTERVAL_MS:2000}
      retry-multiplier: ${COURTSCHEDULER_JMS_RETRY_MULTIPLIER:1.5}
      max-retry-interval-ms: ${COURTSCHEDULER_JMS_MAX_RETRY_INTERVAL_MS:30000}
      connection-ttl-ms: ${COURTSCHEDULER_JMS_CONNECTION_TTL_MS:60000}
      call-timeout-ms: ${COURTSCHEDULER_JMS_CALL_TIMEOUT_MS:15000}
      session-cache-size: ${COURTSCHEDULER_JMS_SESSION_CACHE_SIZE:10}
